"""
Analytics API endpoints for tracking visitor statistics

NOTE: This module is incomplete and disabled until VisitorSession model is implemented.
The router is defined but contains no active endpoints.
"""

import logging

from fastapi import APIRouter

logger = logging.getLogger(__name__)
router = APIRouter()

# Analytics endpoints disabled - awaiting VisitorSession model implementation
# See: app/models/analytics.py for model requirements
